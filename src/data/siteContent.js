/**
 * Words animated in the hero section title on initial page load.
 * Edit this array to change the typing sequence — each string gets its own line.
 */
export const HERO_WORDS = ['Explore', 'Interact', 'Discover']
